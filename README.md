# kudjango-python

Django hello world app connected to a mysql database :

Provide a page to list tools (tools => name + url )
Provide the default admin of django to add new tools.

Contains an sh script to launch it using gunicorn
